{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "update": "npx npm-check-updates -u --install always"
  },
  "dependencies": {
    "@hono/standard-validator": "^0.2.1",
    "hono": "^4.11.3",
    "valibot": "^1.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
